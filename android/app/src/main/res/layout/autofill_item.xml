<?xml version="1.0" encoding="utf-8"?>
<!--
  One row of the system's autofill popup.

  This is a RemoteViews layout: it is inflated by the system, over the window of the app that
  asked to be filled. Two consequences shape it.

  1. It never renders a secret. The row carries the entry's title and user name, and that is all
     it is ever given — a password on screen here would be a password on screen in someone else's
     app, outside our FLAG_SECURE window.
  2. The colours come from the inflating theme (?android:attr/textColor*) rather than from the
     Hisn palette. The popup's background is drawn by the system and follows the device's light
     or dark mode, so a fixed copper would be unreadable on one of them. The brand shows up where
     it can be trusted: the shield on the start edge.

  Only views RemoteViews supports appear here (LinearLayout, ImageView, TextView).
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="56dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <ImageView
        android:id="@+id/autofill_icon"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:src="@mipmap/ic_launcher"
        android:contentDescription="@string/app_name" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="14dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/autofill_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/almarai_bold"
            android:textSize="15sp"
            android:textColor="?android:attr/textColorPrimary"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/autofill_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/almarai_regular"
            android:textSize="13sp"
            android:textColor="?android:attr/textColorSecondary"
            android:maxLines="1"
            android:ellipsize="end" />
    </LinearLayout>
</LinearLayout>
